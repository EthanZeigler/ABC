<%= form_with(model: report_group, local: true) do |form| %>
  <% if report_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(report_group.errors.count, "error") %> prohibited this report_group from being saved:</h2>

      <ul>
      <% report_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name%>
    <%= form.text_area :name, :class => 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit :class => 'btn btn-primary' %>
  </div>
<% end %>
